# Shell-Scripting
Windows and Linux scripts

#### Good scripts here

- clone-github-repositories
    A PowerShell script that clones multiple repositories from a list of repositories in a .txt file. The script assumes that the .txt file contains a list of repository URLs, one per line.